const Router = require('express');
const router = new Router();

router.post('/'); //
router.get('/'); //
router.get('/:id'); //отдельно конкретно взятый девайс на подробной информации

module.exports = router;
